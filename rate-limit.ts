export class RateLimiter {private hits=new Map<string,number[]>();constructor(private max=30,private windowMs=60000){}allow(key:string){const now=Date.now();const arr=(this.hits.get(key)??[]).filter(t=>now-t<this.windowMs);if(arr.length>=this.max){this.hits.set(key,arr);return false}arr.push(now);this.hits.set(key,arr);return true}}
