import test from 'node:test';import assert from 'node:assert/strict';import {HashEmbedding,VectorIndex,cosine} from '../dist/rag/vector.js';import {RateLimiter} from '../dist/auth/rate-limit.js';import {DebateEngine} from '../dist/critique/debate.js';
test('vector index returns closest local embedding',async()=>{const e=new HashEmbedding();const a=await e.embed('payroll salary tax');const b=await e.embed('football match');const idx=new VectorIndex();idx.add({id:'a',text:'payroll',vector:a});idx.add({id:'b',text:'football',vector:b});assert.ok(cosine(a,a)>cosine(a,b));assert.equal(idx.search(a,1)[0].item.id,'a')});
test('rate limiter blocks after threshold',()=>{const r=new RateLimiter(2,60000);assert.equal(r.allow('x'),true);assert.equal(r.allow('x'),true);assert.equal(r.allow('x'),false)});
test('debate detects disagreement',()=>{const d=new DebateEngine().review([{expert:'HR',text:'A',confidence:.8,evidence:[]},{expert:'Tax',text:'B',confidence:.7,evidence:[]}]);assert.equal(d.disagreements.length,1);assert.ok(d.score>0)});
