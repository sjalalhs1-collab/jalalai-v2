import type {ToolContext} from '../tools/registry.js';
import {assertPublicHttpUrl} from '../security/production.js';
export async function browserAutomate(input:unknown,ctx:ToolContext){if(!ctx.permissions.web)throw new Error('Web permission denied');const spec=typeof input==='object'&&input?input as any:{};const url=String(spec.url??'');assertPublicHttpUrl(url);let playwright:any;try{playwright=await (0,eval)('import("playwright")')}catch{throw new Error('playwright_not_installed_optional_dependency')}const browser=await playwright.chromium.launch({headless:true});try{const page=await browser.newPage();await page.goto(url,{waitUntil:'domcontentloaded',timeout:30000});if(Array.isArray(spec.actions)){if(spec.actions.length>20)throw new Error('too_many_browser_actions');for(const a of spec.actions){if(a.type==='click')await page.locator(String(a.selector)).click({timeout:10000});else if(a.type==='fill')await page.locator(String(a.selector)).fill(String(a.value??''));else if(a.type==='wait')await page.waitForTimeout(Math.min(10000,Math.max(0,Number(a.ms??500))));}}return {url:page.url(),title:await page.title(),text:(await page.locator('body').innerText()).slice(0,100000)}}finally{await browser.close()}}
